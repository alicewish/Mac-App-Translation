<sheet version="1">
<markup version="1" identifier="markdownxl" displayName="Markdown XL">
	<tag definition="heading1" pattern="#"></tag>
	<tag definition="heading2" pattern="##"></tag>
	<tag definition="heading3" pattern="###"></tag>
	<tag definition="heading4" pattern="####"></tag>
	<tag definition="heading5" pattern="#####"></tag>
	<tag definition="heading6" pattern="######"></tag>
	<tag definition="codeblock" pattern="''"></tag>
	<tag definition="comment" pattern="%%"></tag>
	<tag definition="divider" pattern="----"></tag>
	<tag definition="nativeblock" pattern="~~"></tag>
	<tag definition="blockquote" pattern="&gt;"></tag>
	<tag definition="orderedList" pattern="\d."></tag>
	<tag definition="unorderedList" pattern="*"></tag>
	<tag definition="unorderedList" pattern="+"></tag>
	<tag definition="unorderedList" pattern="-"></tag>
	<tag definition="code" startPattern="`" endPattern="`"></tag>
	<tag definition="delete" startPattern="||" endPattern="||"></tag>
	<tag definition="emph" startPattern="*" endPattern="*"></tag>
	<tag definition="emph" startPattern="_" endPattern="_"></tag>
	<tag definition="inlineComment" startPattern="++" endPattern="++"></tag>
	<tag definition="inlineNative" startPattern="~" endPattern="~"></tag>
	<tag definition="mark" startPattern="::" endPattern="::"></tag>
	<tag definition="strong" startPattern="__" endPattern="__"></tag>
	<tag definition="strong" startPattern="**" endPattern="**"></tag>
	<tag definition="annotation" startPattern="{" endPattern="}"></tag>
	<tag definition="link" startPattern="[" endPattern="]"></tag>
	<tag definition="footnote" pattern="(fn)"></tag>
	<tag definition="image" pattern="(img)"></tag>
	<tag definition="video" pattern="(vid)"></tag>
</markup>
<string xml:space="preserve">
<p><tags><tag kind="heading2">## </tag></tags>Sheets</p>
<p></p>
<p>All writing is done in Ulysses’ sheets. Sheets are somewhat equivalent to classic documents, though they don’t require a “title” or a “file name” or such.</p>
<p></p>
<p>You create sheets via <element kind="code" startTag="`">⌘N</element> or the new sheet button in toolbar.</p>
<p></p>
<p>Sheets can hold any amount of text, and you can freely move sheets around and sort them at will. You can also select multiple sheets and glue them together (<element kind="code" startTag="`">⌘J</element>). They will then behave as a single sheet in the editor, which is especially cool if you rather have a lot of small chunks than a single, monolithic wall of text.</p>
<p></p>
<p>You can also split a sheet in two at the current cursor position (Edit › Split at Selection), and merge two or more sheets together to form one (File › Merge Sheets).</p>
<p></p>
<p></p>
<p><tags><tag kind="heading3">### </tag></tags>Added bonus</p>
<p></p>
<p>Since sheets are likely to live in the vicinity of other sheets, we figured it would be nice if you could easily navigate between them — right from within the editor. There are keyboard shortcuts for this (<element kind="code" startTag="`">⌥⌘↓/↑</element>).</p>
<p></p>
<p>You should try this now to get the hang of it. Just come back here, as we’re about to look at the cool stuff.</p>
<p></p></string>
</sheet>